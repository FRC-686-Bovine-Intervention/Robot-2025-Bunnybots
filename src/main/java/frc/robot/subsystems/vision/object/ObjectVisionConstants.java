package frc.robot.subsystems.vision.object;

import java.util.HashMap;
import java.util.Map;

import edu.wpi.first.math.geometry.Translation2d;
import edu.wpi.first.math.geometry.Translation3d;
import frc.robot.constants.FieldConstants;

public class ObjectVisionConstants {
    /*
    public static final ObjectType lunite = new ObjectType(
        "Lunite",
        true,
        FieldConstants.luniteIntakeLocations.getOurs(),
        FieldConstants.luniteDimensions
    );

    // INDEX MUST ALIGN WITH MODEL INDEX
    public static final ObjectType[] objectTypes = {
        lunite
    };

    public static class ObjectType {
        public final String name;
        public final boolean intakeable;
        public final Translation2d[] memoryLocations;
        public final Translation3d objectDimensions; //As it would lay on ground: x, y, z dimensions
        public ObjectType(String name, boolean intakeable, Translation2d[] memoryLocations, Translation3d objectDimensions) {
            this.name = name;
            this.intakeable = intakeable;
            this.memoryLocations = memoryLocations;
            this.objectDimensions = objectDimensions;
        }
    }
    */
}
